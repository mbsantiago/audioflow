profiles {
    standard {
        process.executor = 'local'
    }

    cluster {
        executor {
            name = 'sge'
            queueSize = 20
            pollInterval = '30 sec'
        }
    }
}
